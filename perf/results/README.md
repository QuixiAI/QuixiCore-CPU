# Raw Performance Results

Store small benchmark logs and summarized raw outputs here when they are needed
to support entries in `perf/optimization_status.md` or
`perf/baseline_status.md`.

Do not commit large profiler traces.

